{
    "name": "example-api",
    "version": "1.0.0",
    "description": "A simple API whose reverse proxy enforces Mutual TLS and certificate bound access tokens",
    "main": "server.ts",
    "scripts": {
        "build": "rimraf dist && tsc",
        "startApi": "node dist/server.js",
        "start": "npm run build && npm run startApi"
    },
    "author": "Curity AB",
    "license": "Apache-2.0",
    "dependencies": {
        "express": "^4.18.2",
        "jose": "^5.1.2"
    },
    "devDependencies": {
        "@types/express": "^4.17.21",
        "@types/node": "^20.10.0",
        "rimraf": "^5.0.5",
        "typescript": "^5.3.2"
    }
}
