{
    "name": "example-api",
    "version": "1.0.0",
    "description": "An API that validates JWTs and also verifies the Mutual TLS public key in the cnf claim",
    "main": "server.ts",
    "scripts": {
        "build": "rimraf dist && tsc",
        "start": "npm run build && node dist/server.js"
    },
    "author": "Curity AB",
    "license": "Apache-2.0",
    "dependencies": {
        "express": "^4.17.1",
        "jose": "^3.15.4"
    },
    "devDependencies": {
        "@types/express": "^4.17.9",
        "@types/node": "^14.14.10",
        "rimraf": "^3.0.2",
        "typescript": "^4.3.5"
    }
}
