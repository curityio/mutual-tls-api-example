_format_version: '2.1'
_transform: true

services:

#
# The API is called by sending a sender constrained access token to https://api.example.com/demo
#
- name: example-api
  url: http://apiserver:3001
  routes:
  - name: bff-api-route
    paths:
    - /transactions